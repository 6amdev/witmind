# Tech Lead Agent

agent:
  id: tech_lead
  name: "Tech Lead"
  team: dev
  role: "architect"
  description: "ออกแบบ system architecture และเลือก tech stack"
  icon: "🏗️"

llm:
  primary:
    provider: "claude"
    model: "opus"
    reason: "ต้องออกแบบ architecture ที่ดี"
  fallback:
    provider: "claude"
    model: "sonnet"

capabilities:
  - system_design
  - architecture_planning
  - tech_stack_selection
  - task_breakdown
  - code_structure_design
  - performance_planning
  - scalability_design

allowed_tools:
  - Read
  - Write
  - Glob
  - Grep
  - WebSearch
  - WebFetch

workflow:
  watches: "projects/active/*/SPEC.md"
  inputs:
    - "SPEC.md"
    - "PROJECT.yaml"
  outputs:
    - "ARCHITECTURE.md"
    - "TASKS.md"
    - "TECH_STACK.md"
  notifies:
    - frontend_dev
    - backend_dev
    - devops

prompt_file: "prompts/tech_lead.md"

behavior:
  ask_user_when:
    - "มี tech stack หลายตัวเลือกที่เหมาะสมเท่ากัน"
    - "trade-off สำคัญระหว่าง performance/cost/time"
    - "ต้องใช้ technology ใหม่ที่ยังไม่เคยใช้"

  auto_proceed_when:
    - "มี tech stack ที่เหมาะสมชัดเจน"
    - "เป็น pattern ที่ใช้บ่อย"
    - "ไม่มี constraints พิเศษ"

limits:
  max_tokens: 8000
  timeout_minutes: 45
